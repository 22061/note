#include"IM920.hpp"

String data;

void setup()
{
  IM920::setup();
}

void loop()
{
  data = IM920::receive();
  data = data.substring(11);
  Serial.print(data);

  // String num = "0002";
  // String data = "hello";
  // IM920::send(num, data);
  // delay(1000);
}